# Configuración de la base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_contraseña_aqui
DB_NAME=db_voluntariado

# Puerto del servidor
PORT=3000

# JWT Secret para autenticación (cambia esto por una clave segura)
JWT_SECRET=tu_clave_secreta_super_segura_aqui_12345

# Entorno
NODE_ENV=development

# Configuración de JWT
JWT_EXPIRES_IN=24h

# MongoDB Atlas (para fotos de perfil)
MONGODB_USER=leocruzhn_db_user
MONGODB_PASSWORD=RYfaRRbdka6rkBSO
MONGODB_CLUSTER=cluster0.xxxxx.mongodb.net
MONGODB_DATABASE=db_voluntariado_archivos

# O usa la URI completa directamente:
# MONGODB_URI=mongodb+srv://leocruzhn_db_user:RYfaRRbdka6rkBSO@cluster0.xxxxx.mongodb.net/db_voluntariado_archivos?retryWrites=true&w=majority


# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo a ".env"
# 2. Reemplaza "tu_contraseña_aqui" con tu contraseña de MySQL
# 3. Cambia JWT_SECRET por una clave segura aleatoria
# 4. Ve a MongoDB Atlas y copia tu connection string (cluster URL)
# 5. Reemplaza "cluster0.xxxxx.mongodb.net" con tu cluster real
# 6. Guarda el archivo

